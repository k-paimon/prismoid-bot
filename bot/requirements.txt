# No third-party dependencies — the bot, exchange clients, backtester, and
# cloud agent are Python standard library only (urllib, json, threading, ...).
# Python 3.9+ recommended. `pip install -r requirements.txt` is a no-op.
